import styles from './button.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function Button() {
  return <button style={{}}>Login</button>;
}

export default Button;
